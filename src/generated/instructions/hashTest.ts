/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */

import * as beet from '@metaplex-foundation/beet'
import * as web3 from '@solana/web3.js'
import { HashTuple, hashTupleBeet } from '../types/HashTuple'

/**
 * @category Instructions
 * @category HashTest
 * @category generated
 */
export type HashTestInstructionArgs = {
  hashList: HashTuple[]
}
/**
 * @category Instructions
 * @category HashTest
 * @category generated
 */
export const hashTestStruct = new beet.FixableBeetArgsStruct<
  HashTestInstructionArgs & {
    instructionDiscriminator: number[] /* size: 8 */
  }
>(
  [
    ['instructionDiscriminator', beet.uniformFixedSizeArray(beet.u8, 8)],
    ['hashList', beet.array(hashTupleBeet)],
  ],
  'HashTestInstructionArgs'
)
/**
 * Accounts required by the _hashTest_ instruction
 *
 * @property [_writable_, **signer**] signer
 * @category Instructions
 * @category HashTest
 * @category generated
 */
export type HashTestInstructionAccounts = {
  signer: web3.PublicKey
  anchorRemainingAccounts?: web3.AccountMeta[]
}

export const hashTestInstructionDiscriminator = [
  75, 196, 116, 236, 50, 243, 121, 103,
]

/**
 * Creates a _HashTest_ instruction.
 *
 * @param accounts that will be accessed while the instruction is processed
 * @param args to provide as instruction data to the program
 *
 * @category Instructions
 * @category HashTest
 * @category generated
 */
export function createHashTestInstruction(
  accounts: HashTestInstructionAccounts,
  args: HashTestInstructionArgs,
  programId = new web3.PublicKey('undefined')
) {
  const [data] = hashTestStruct.serialize({
    instructionDiscriminator: hashTestInstructionDiscriminator,
    ...args,
  })
  const keys: web3.AccountMeta[] = [
    {
      pubkey: accounts.signer,
      isWritable: true,
      isSigner: true,
    },
  ]

  if (accounts.anchorRemainingAccounts != null) {
    for (const acc of accounts.anchorRemainingAccounts) {
      keys.push(acc)
    }
  }

  const ix = new web3.TransactionInstruction({
    programId,
    keys,
    data,
  })
  return ix
}
